#' Todos
#'
#'
#' @docType data
#' @name todo
todo <-
  list(
    producer =
      list(
      ),
    consumer =
      list(
        "consumer: allow props to be set within initialize",
        "change offset: i",
        "change offset by time"
      ),
    admin =
      list(
        "wrap admin api"
      ),
    general =
      list(
        "testing",
        "continous integration testing"
    )
  )


